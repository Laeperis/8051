C51 COMPILER V9.59.0.0   MAIN                                                              07/11/2025 21:36:36 PAGE 1   


C51 COMPILER V9.59.0.0, COMPILATION OF MODULE MAIN
OBJECT MODULE PLACED IN .\Objects\main.obj
COMPILER INVOKED BY: F:\Keil uvision5 C51v959(64bit)\C51\BIN\C51.EXE test\test\main.c BROWSE DEBUG OBJECTEXTEND PRINT(.\
                    -Listings\main.lst) OBJECT(.\Objects\main.obj)

line level    source

   1          #include <reg51.h>
   2          #include <stdio.h>
   3          #include "lcd1602.h"
   4          #include "dht11.h"
   5          #include <string.h>
   6          #include <stdlib.h>
   7          #include <intrins.h> // 修正 _nop_ 错误
   8          #define u8 unsigned char // 修正 u8 未定义
   9          
  10          void UART_SendStr(char *str); // 添加函数原型声明，防止编译器警告
  11          
  12          sbit KEY = P3^2; // 启动/停止按键
  13          
  14          bit collect_flag = 0; // 采集标志
  15          volatile char last_rx = 0;
  16          volatile bit rx_flag = 0;
  17          // char debug[17]; // 全局变量（删除，调试用局部变量）
  18          
  19          // 优化：用于接收上位机回发的数字，缩短为6字节
  20          unsigned char xdata num_buf[12]; // 放到xdata区，减轻data压力
  21          unsigned char num_idx = 0;
  22          
  23          volatile unsigned char current_channel = 0; // 0=DHT11, 1=555频率
  24          volatile bit freq_sample_flag = 0;
  25          volatile unsigned char t0_count = 0;
  26          
  27          // -- 频率测量所需的状态变量 --
  28          volatile unsigned int freq_count = 0; // 用于累加脉冲数量
  29          unsigned int freq_value = 0;        // 用于存放最终计算出的频率值
  30          static bit last_p32_state = 1;      // 用于检测P3.2引脚的下降沿
  31          
  32          // I2C/PCF8591相关定义
  33          sbit IIC_SDA = P1^1;
  34          sbit IIC_SCL = P1^0;
  35          #define PCF8591_WRITE_ADDR 0x90
  36          #define PCF8591_READ_ADDR  0x91
  37          //报警LED定义
  38          sbit LED1 = P1^2;
  39          sbit LED2 = P1^3;
  40          sbit LED3 = P1^4;
  41          
  42          void IIC_Delay() { _nop_(); _nop_(); _nop_(); }
  43          void IIC_SendStart(void) {
  44   1          IIC_SDA=1; IIC_SCL=1; IIC_Delay();
  45   1          IIC_SDA=0; IIC_Delay();
  46   1          IIC_SCL=0;
  47   1      }
  48          void IIC_SendStop(void) {
  49   1          IIC_SCL=0; IIC_SDA=0; IIC_Delay();
  50   1          IIC_SCL=1; IIC_SDA=1; IIC_Delay();
  51   1      }
  52          u8 IIC_GetAck(void) {
  53   1          u8 i=0; IIC_SDA=1; IIC_SCL=1;
  54   1          while(IIC_SDA) { i++; if(i>250) { IIC_SCL=0; return 1; } }
C51 COMPILER V9.59.0.0   MAIN                                                              07/11/2025 21:36:36 PAGE 2   

  55   1          IIC_SCL=0; return 0;
  56   1      }
  57          void IIC_SendOneByte(u8 dat) {
  58   1          u8 j;
  59   1          for(j=0;j<8;j++) {
  60   2              IIC_SCL=0;
  61   2              IIC_SDA=(dat&0x80)?1:0;
  62   2              dat<<=1;
  63   2              IIC_SCL=1; IIC_Delay();
  64   2          }
  65   1          IIC_SCL=0;
  66   1      }
  67          void PCF8591_SetDAC_Data(u8 val) {
  68   1          IIC_SendStart();
  69   1          IIC_SendOneByte(PCF8591_WRITE_ADDR);
  70   1          IIC_GetAck();
  71   1          IIC_SendOneByte(0x40); // 控制字节：DAC使能
  72   1          IIC_GetAck();
  73   1          IIC_SendOneByte(val);
  74   1          IIC_GetAck();
  75   1          IIC_SendStop();
  76   1      }
  77          void Delay100ms() {
  78   1          unsigned char i, j;
  79   1          for(i=0;i<20;i++) {
  80   2              for(j=0;j<250;j++); // 1us*250*20=5ms*20=100ms
  81   2          }
  82   1      }
  83          
  84          // 解析并输出温湿度减半值到DAC
  85          void handle_half_value(const char* str) {
  86   1          int t = 0, h = 0;
  87   1          UART_SendStr("[DEBUG] RAW BUF: ");
  88   1          UART_SendStr(str);
  89   1          UART_SendStr("\r\n");
  90   1          sscanf(str, "%d %d", &t, &h);
  91   1          // 先输出温度
  92   1          PCF8591_SetDAC_Data((unsigned char)t);
  93   1          UART_SendStr("[DEBUG] DAC OUT TEMP: ");
  94   1          {
  95   2              char xdata buf[8]; // 放到xdata区
  96   2              unsigned int temp_val = (unsigned char)t;
  97   2              sprintf(buf, "%u\r\n", temp_val);
  98   2              UART_SendStr(buf);
  99   2          }
 100   1          Delay100ms();
 101   1          // 再输出湿度
 102   1          PCF8591_SetDAC_Data((unsigned char)h);
 103   1          UART_SendStr("[DEBUG] DAC OUT HUMI: ");
 104   1          {
 105   2              char xdata buf[8]; // 放到xdata区
 106   2              unsigned int humi_val = (unsigned char)h;
 107   2              sprintf(buf, "%u\r\n", humi_val);
 108   2              UART_SendStr(buf);
 109   2          }
 110   1          Delay100ms();
 111   1      }
 112          
 113          // 新增：解析并输出减半频率值到DAC
 114          void handle_half_freq(const char* str) {
 115   1          int f = 0;
 116   1          UART_SendStr("[DEBUG] RAW FREQ BUF: ");
C51 COMPILER V9.59.0.0   MAIN                                                              07/11/2025 21:36:36 PAGE 3   

 117   1          UART_SendStr(str);
 118   1          UART_SendStr("\r\n");
 119   1          sscanf(str, "%d", &f);
 120   1          PCF8591_SetDAC_Data((unsigned char)f);
 121   1          UART_SendStr("[DEBUG] DAC OUT FREQ: ");
 122   1          {
 123   2              char xdata buf[8];
 124   2              unsigned int freq_val = (unsigned char)f;
 125   2              sprintf(buf, "%u\r\n", freq_val);
 126   2              UART_SendStr(buf);
 127   2          }
 128   1          Delay100ms();
 129   1      }
 130          
 131          void UART_Init() {
 132   1          SCON = 0x50;      // 8位数据,可变波特率
 133   1          TMOD |= 0x20;     // 定时器1，8位自动重装
 134   1          TH1 = 0xFD;       // 9600波特率
 135   1          TL1 = 0xFD;
 136   1          TR1 = 1;
 137   1          ES = 1;
 138   1          EA = 1;
 139   1      }
 140          
 141          void Timer0_Init() {
 142   1          TMOD &= 0xF0;
 143   1          TMOD |= 0x01; // T0方式1
 144   1          TH0 = (65536 - 50000) / 256; // 50ms定时
 145   1          TL0 = (65536 - 50000) % 256;
 146   1          ET0 = 1;
 147   1          TR0 = 1;
 148   1      }
 149          
 150          void Timer1_Init() {
 151   1          TMOD &= 0x0F;
 152   1          TMOD |= 0x50; // T1方式1，计数器模式
 153   1          TH1 = 0;
 154   1          TL1 = 0;
 155   1          TR1 = 1;
 156   1      }
 157          
 158          void INT0_Init() {
 159   1          IT0 = 1; // 下降沿触发
 160   1      }
 161          
 162          void UART_SendStr(char *str) {
 163   1          while(*str) {
 164   2              SBUF = *str++;
 165   2              while(!TI);
 166   2              TI = 0;
 167   2          }
 168   1      }
 169          
 170          void UART_ISR() interrupt 4 {
 171   1          if(RI) {
 172   2              char ch = SBUF;
 173   2              RI = 0;
 174   2              // 接收一行，遇到回车/换行处理
 175   2              if(num_idx < sizeof(num_buf) - 1 && ch != '\r' && ch != '\n') {
 176   3                  num_buf[num_idx++] = ch;
 177   3              }
 178   2              if(ch == '\r' || ch == '\n') {
C51 COMPILER V9.59.0.0   MAIN                                                              07/11/2025 21:36:36 PAGE 4   

 179   3                  if(num_idx > 0) {
 180   4                      num_buf[num_idx] = '\0';
 181   4                      // 判断是否为命令行
 182   4                      if(strncmp((char*)num_buf, "CMD:", 4) == 0 && num_idx >= 5) {
 183   5                          char cmd = num_buf[4];
 184   5                          // 处理命令
 185   5                          if(cmd == 'S') collect_flag = 1;
 186   5                          if(cmd == 'E') collect_flag = 0;
 187   5                          if(cmd == 'A') {
 188   6                              current_channel = 0;
 189   6                              freq_count = 0;
 190   6                              freq_sample_flag = 0;
 191   6                              LCD_ShowString(0,0,"                ");
 192   6                              LCD_ShowString(1,0,"                ");
 193   6                          }
 194   5                          if(cmd == 'B') {
 195   6                              current_channel = 1;
 196   6                              freq_count = 0;
 197   6                              t0_count = 0;
 198   6                              freq_sample_flag = 0;
 199   6                              LCD_ShowString(0,0,"                ");
 200   6                              LCD_ShowString(1,0,"                ");
 201   6                          }
 202   5                          if(cmd == 'X'){
 203   6                              LED1 = 0;
 204   6                              UART_SendStr("TEMPER ALARM\r\n");
 205   6                          }
 206   5                          if(cmd == 'Y'){
 207   6                              LED2 = 0;
 208   6                              UART_SendStr("HUMI ALARM\r\n");
 209   6                          }
 210   5                          if(cmd == 'Z'){
 211   6                              LED3 = 0;
 212   6                              UART_SendStr("FREQ ALARM\r\n");
 213   6                          }
 214   5                          if(cmd == 'x'){
 215   6                              LED1 = 1;
 216   6                              UART_SendStr("TEMPER NORMAL\r\n");
 217   6                          }
 218   5                          if(cmd == 'y'){
 219   6                              LED2 = 1;
 220   6                              UART_SendStr("HUMI NORMAL\r\n");
 221   6                          }
 222   5                          if(cmd == 'z'){
 223   6                              LED3 = 1;
 224   6                              UART_SendStr("FREQ NORMAL\r\n");
 225   6                          }
 226   5                      } else {
 227   5                          // 不是命令，按原有数据处理
 228   5                          UART_SendStr("HALF VALUE: ");
 229   5                          UART_SendStr((char*)num_buf);
 230   5                          UART_SendStr("\r\n");
 231   5                          if(current_channel == 0) {
 232   6                              handle_half_value((char*)num_buf);
 233   6                          } else if(current_channel == 1) {
 234   6                              handle_half_freq((char*)num_buf);
 235   6                          }
 236   5                      }
 237   4                      num_idx = 0;
 238   4                  }
 239   3              }
 240   2          }
C51 COMPILER V9.59.0.0   MAIN                                                              07/11/2025 21:36:36 PAGE 5   

 241   1      }
 242          
 243          void Timer0_ISR() interrupt 1 {
 244   1          TH0 = (65536 - 50000) / 256;
 245   1          TL0 = (65536 - 50000) % 256;
 246   1          t0_count++;
 247   1          if (t0_count >= 20) { // 20 * 50ms = 1000ms = 1s
 248   2              t0_count = 0;
 249   2              freq_sample_flag = 1; // 产生1s采样标志
 250   2          }
 251   1      }
 252          
 253          void INT0_ISR() interrupt 0 {
 254   1          freq_count++;
 255   1      }
 256          
 257          extern void Delay1000ms();
 258          sbit FREQ_IN = P3^2;
 259          
 260          void main() {
 261   1          // 简化局部变量，只保留必要的
 262   1          unsigned char temp, humi;
 263   1          char xdata buf[20]; // 放到xdata区
 264   1      
 265   1          UART_SendStr("[DEBUG] UART_Init\r\n");
 266   1          UART_Init();
 267   1          UART_SendStr("[DEBUG] LCD_Init\r\n");
 268   1          LCD_Init();
 269   1          UART_SendStr("[DEBUG] Timer0_Init\r\n");
 270   1          Timer0_Init();
 271   1          UART_SendStr("[DEBUG] INT0_Init\r\n");
 272   1          INT0_Init();
 273   1          EA = 1; // 总中断使能
 274   1          UART_SendStr("[DEBUG] Init Done\r\n");
 275   1          LCD_ShowString(0,0,"WAIT CMD      ");
 276   1          while(1) {
 277   2              // --- 硬件状态控制器 ---
 278   2              // 根据软件状态，实时决定是否开启外部中断进行频率计数
 279   2              if (collect_flag && current_channel == 1) {
 280   3                  EX0 = 1; // 启动采集且在频率通道时，使能外部中断
 281   3              } else {
 282   3                  EX0 = 0; // 其他所有情况（停止或在温湿度通道），都关闭外部中断
 283   3              }
 284   2      
 285   2              if(collect_flag) {
 286   3                  if(current_channel == 1) { // 555频率测量 (硬件中断计数法)
 287   4                      // 轮询代码已删除，计数在后台由INT0_ISR自动完成
 288   4                      if(freq_sample_flag) {
 289   5                         
 290   5                          freq_sample_flag = 0; // 清除标志，为下个周期做准备
 291   5      
 292   5                          EA = 0; // 关总中断，保证原子操作
 293   5                          freq_value = freq_count; // 1s内的计数值就是频率
 294   5                          freq_count = 0;          // 将脉冲计数器清零，开始新的计数周期
 295   5                          EA = 1; // 开总中断
 296   5      
 297   5                          LCD_ShowString(0,0,"FREQ:       Hz");
 298   5                          LCD_ShowNum(0,6,freq_value,5);
 299   5                          sprintf(buf, "FREQ:%u\r\n", freq_value);
 300   5                          UART_SendStr(buf);
 301   5                      }
 302   4                  } else if(current_channel == 0) { // DHT11温湿度
C51 COMPILER V9.59.0.0   MAIN                                                              07/11/2025 21:36:36 PAGE 6   

 303   4                      // freq_count = 0; // 确保在DHT11模式下，频率计数器是清零的
 304   4                      if (freq_sample_flag) { // 复用1秒的定时器门控
 305   5                          
 306   5                          freq_sample_flag = 0;
 307   5                          
 308   5                          EA = 0; // 关闭总中断
 309   5                          if(DHT11_Read(&temp, &humi) == 0) {
 310   6                              EA = 1; // 恢复总中断
 311   6                              LCD_ShowString(0,0,"Temp:    C");
 312   6                              LCD_ShowString(1,0,"Humi:    %");
 313   6                              LCD_ShowNum(0,6,temp,2);
 314   6                              LCD_ShowNum(1,6,humi,2);
 315   6                              sprintf(buf, "T:%u H:%u\r\n", (unsigned int)temp, (unsigned int)humi);
 316   6                              UART_SendStr(buf);
 317   6                          } else {
 318   6                              EA = 1; // 恢复总中断
 319   6                              UART_SendStr("[DEBUG] DHT11 FAIL\r\n");
 320   6                              LCD_ShowString(0,0,"Temp:    C");
 321   6                              LCD_ShowString(1,0,"Humi:    %");
 322   6                              LCD_ShowNum(0,6,99,2);
 323   6                              LCD_ShowNum(1,6,99,2);
 324   6                              UART_SendStr("DHT11 FAIL\r\n");
 325   6                          }
 326   5                      }
 327   4                  }
 328   3              } else {
 329   3                  freq_count = 0; // 停止采集时，也清零频率计数器
 330   3                  LCD_ShowString(0,0,"WAIT CMD      ");
 331   3                  LCD_ShowString(1,0,"              ");
 332   3                  Delay1000ms();
 333   3              }
 334   2              Delay100ms(); // 新增：每次主循环末尾短暂延时，降低CPU占用
 335   2          }
 336   1      } 


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =   1474    ----
   CONSTANT SIZE    =    471    ----
   XDATA SIZE       =     12      44
   PDATA SIZE       =   ----    ----
   DATA SIZE        =      8      23
   IDATA SIZE       =   ----    ----
   BIT SIZE         =      4    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
